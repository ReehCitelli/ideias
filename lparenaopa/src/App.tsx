/**
 * ⚠️ ESTE ARQUIVO NÃO É USADO PELO SITE.
 *
 * A landing page da Núcleo Motopeças foi entregue, por pedido, como um
 * ÚNICO ARQUIVO HTML AUTOCONTIDO — sem framework e sem etapa de build:
 *
 *      ->  /index.html        (o site inteiro: HTML + CSS + JS embutidos)
 *      ->  /LEIA-ME.md        (configuração, deploy e checklist)
 *      ->  /google-apps-script/Codigo.gs  (recebe o formulário na planilha)
 *
 * O `index.html` da raiz não importa este módulo. Rodar `npm run build`
 * apenas copia/otimiza esse mesmo HTML para `dist/index.html`, que continua
 * sendo um arquivo único que funciona só abrindo no navegador (file://).
 *
 * Este componente existe somente para manter a estrutura do projeto.
 */
export default function App() {
  return null;
}
